name: RSD hidden-state forecast

on:
  workflow_dispatch:
  push:
    branches:
      - inverse-rsd-upgrade
    paths:
      - 'code/rsd_hidden_state_forecast.py'
      - 'code/class_response_optimize.py'
      - '.github/workflows/rsd_hidden_state_forecast.yml'

jobs:
  rsd-forecast:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout manuscript repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Python dependencies
        run: python -m pip install --upgrade pip setuptools wheel numpy cython

      - name: Fetch pinned CLASS
        run: |
          git init class_public
          git -C class_public remote add origin https://github.com/lesgourg/class_public.git
          git -C class_public fetch --depth 1 origin e85808324f51fc694d12e3ed7439552a3c3f9540
          git -C class_public checkout --detach FETCH_HEAD
          test "$(git -C class_public rev-parse HEAD)" = "e85808324f51fc694d12e3ed7439552a3c3f9540"

      - name: Install pinned CLASS Python wrapper
        run: python -m pip install ./class_public

      - name: Run projected RSD optimization
        run: |
          python code/rsd_hidden_state_forecast.py \
            --outdir rsd_hidden_state_output \
            --mass 0.60 \
            --z-match 1100 \
            --probe-frac 0.01 \
            --final-frac 0.30 \
            --volume 50 \
            --samples 20000 \
            --seed 20260913

      - name: Record solver commit
        if: always()
        run: git -C class_public rev-parse HEAD > rsd_hidden_state_output/CLASS_COMMIT.txt

      - name: Upload RSD artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: rsd-hidden-state-forecast
          path: rsd_hidden_state_output/
          if-no-files-found: error
